// Function 6:
// even or odd?  will take in one parameter ( a number ) and print either even or odd, depending on the number. 

const isEven = (even) => even % 2 === 0 ? `${even} is Odd` : `${even} is Even`; 

console.log(isEven(24));